<%= render 'layouts/navbar'%>

<%= render 'layouts/modale'%>



<div class="wrap-list">
  <div id='liste'>
    <%= image_tag("photos/purple(2).jpg", class: "img-bg") %>
<%@places.reverse.each_with_index do |fact,index|%>
  <div id="list_show">

    <% if @ids.include?(fact.id) %>

    <i class="fa fa-heart" id="fav" onclick="tamer(<%= fact.id %>, <%= current_user.id %>)" style="color: rgb(255,0,0);" ></i>
    <% else %>
    <i class="fa fa-heart" id="fav" onclick="tamer(<%= fact.id %>, <%= current_user.id %>)" style="color: rgb(128,128,128);" ></i>  
  <%end %>
        <h3><%=fact.name%></h3>
        <hr>
        <p class="shop_body"><%= fact.description %></p>
        <p><%=fact.address%></p>
    </div>
<% end %>
  </div>
</div>
<%= javascript_include_tag 'list' %> 

  <script>
  </script>
